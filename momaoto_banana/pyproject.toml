[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aihunt"
version = "0.1.0"
description = "Phase 1 MVP for climbing scene to avatar animation"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["aihunt"]
